# @package _global_
# Experimento ALPR 48x96 - ALPR Fast (Pure CNN, embed 256, 1 refine, balanceado)
experiment_name: exp_48x96_parseq_cnn_alpr_fast
defaults:
  - override /model: parseq_cnn_alpr_fast
  - override /charset: 36_uppercase
  - override /dataset: VeSV_pad_48x96

model:
  img_size: [ 48, 96 ]
  max_label_length: 8
