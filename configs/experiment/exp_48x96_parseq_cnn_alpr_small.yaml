# @package _global_
# Experimento ALPR 48x96 - ALPR Small (Hybrid CNN+2ViT, embed 256, alta acurácia)
experiment_name: exp_48x96_parseq_cnn_alpr_small
defaults:
  - override /model: parseq_cnn_alpr_small
  - override /charset: 36_uppercase
  - override /dataset: VeSV_pad_48x96

model:
  img_size: [ 48, 96 ]
  max_label_length: 8
