# @package _global_
experiment_name: parseq-patch16-224
experiment: parseq-patch16-224
defaults:
  - override /model: parseq

model:
  img_size: [ 224, 224 ]  # [ height, width ]
  patch_size: [ 16, 16 ]  # [ height, width ]
