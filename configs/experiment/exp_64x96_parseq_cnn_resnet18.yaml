# @package _global_
# Experimento ALPR 64x96 - ResNet-18 Pure CNN
experiment_name: exp_64x96_parseq_cnn_resnet18
defaults:
  - override /model: parseq_cnn_resnet18
  - override /charset: 36_uppercase
  - override /dataset: VeSV_pad_64x96

model:
  img_size: [ 64, 96 ]
  max_label_length: 8
