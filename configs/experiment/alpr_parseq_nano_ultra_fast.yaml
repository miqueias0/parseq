# @package _global_
experiment_name: alpr_parseq_nano_ultra_fast
experiment: alpr_parseq_nano_ultra_fast
# PARSeq Nano ALPR - Ultra-Fast (Latência Mínima para CPU / Edge)
# embed_dim: 192, enc_depth: 4, enc_mlp_ratio: 2, refine_iters: 0, decode_ar: false
defaults:
  - override /model: parseq_alpr_nano
  - override /charset: 36_uppercase

model:
  img_size: [ 48, 96 ]
  patch_size: [ 6, 8 ]
  max_label_length: 8
