# @package _global_
# Experimento ALPR 48x96 - parseq_alpr_nano (1.70M params)
# Dataset: VeSV_pad (Padrao unificado carros/motos)
experiment_name: exp_48x96_parseq_alpr_nano
defaults:
  - override /model: parseq_alpr_nano
  - override /charset: 36_uppercase
  - override /dataset: VeSV_pad_48x96

model:
  img_size: [ 48, 96 ]
  max_label_length: 8
