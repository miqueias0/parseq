# @package _global_
# Experimento ALPR 48x96 - RepVGG Hybrid (4 ViT blocks)
experiment_name: exp_48x96_parseq_cnn_repvgg_hybrid
defaults:
  - override /model: parseq_cnn_repvgg_hybrid
  - override /charset: 36_uppercase
  - override /dataset: VeSV_pad_48x96

model:
  img_size: [ 48, 96 ]
  max_label_length: 8
