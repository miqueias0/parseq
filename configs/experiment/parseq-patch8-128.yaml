# @package _global_
experiment_name: parseq-patch8-128
experiment: parseq-patch8-128
defaults:
  - override /model: parseq

model:
  img_size: [ 128, 128 ]  # [ height, width ]
  patch_size: [ 8, 8 ]  # [ height, width ]
