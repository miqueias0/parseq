# @package _global_
# Experimento ALPR 64x96 - ALPR Fast (Pure CNN, embed 256, 1 refine)
experiment_name: exp_64x96_parseq_cnn_alpr_fast
defaults:
  - override /model: parseq_cnn_alpr_fast
  - override /charset: 36_uppercase
  - override /dataset: VeSV_pad_64x96

model:
  img_size: [ 64, 96 ]
  max_label_length: 8
